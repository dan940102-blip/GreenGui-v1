
local Window = Rayfield:CreateWindow({
    Name = "GreenGui V1"
    LoadingTitle = "Welcome Back To Exploit Mr.DanHang"
    LoadingSubtitle = "Lets Do Some Trolling >:)"
    ConfigurationSaving = {
      Enabled = false,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "GreenGui"
    },
    KeySystem = true, -- Set this to true to use our key system
    KeySettings = {
       Title = "Please Enter Your Key"
       Subtitle = "Verifying Its You Mr.DanHang"
       Note = "Start Trolling After You Enter The Key"
       FileName = "YouLittleScripter", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
       SaveKey = false, -- The user's key will be saved, but if you change the key, they will be unable to use your script
        }
    })
local MainTab = Window:CreateTab("ðŸŸ¢Main, nil") -- Title, Image
local MainSection = MainTab:CreateSection("ðŸŸ¢Main")

Rayfield:Notify({
   Title = "Script Loaded",
   Content = "Lets Start Exploiting $:)"
   Duration = 5
   Image = 8671252706969f68,
   Actions = { -- Notification Buttons
      Ignore = {
         Name = "Alright"
         Callback = function()
         print("The User Tapped Alright")
                end
            },
        },
    })
local Button = MainTab:CreateButton({
   Name = "Infinite Yield",
   Callback = function()
       --Toggles the Infinite Yield between on or off on every script run
loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
if loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))() == nil then
                --Ensures this only runs once to save resources
                loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))() = true
                --Notifies readiness
                game starterGui:SetCore("SendNotification", {Title="GreenGui"; Text="Infinite Yield Loaded"; Duration=5;})
                end
		end
	end)
end
   end,
